from PIL import Image

def apply_style_transfer(content_img: Image.Image, style_img: Image.Image) -> Image.Image:
    # Placeholder: returns resized content image
    content_img = content_img.resize((512, 512))
    return content_img
